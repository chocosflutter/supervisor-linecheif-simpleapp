# Copy to .env and fill in. The anon/publishable key is public by design (RLS is the boundary).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# "mock" (in-memory, no backend) or "supabase" (live DB; requires auth session for RLS).
VITE_DATA_SOURCE=mock
